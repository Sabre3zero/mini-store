export type AppCardTypes = {
  image: string;
  title: string;
  text: string;
};

export type AppsTypes = {
  id: number;
  cover: {
    url: string;
  };
  title: string;
  description: string;
  price: number;
};
